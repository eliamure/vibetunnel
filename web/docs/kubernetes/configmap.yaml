apiVersion: v1
kind: ConfigMap
metadata:
  name: vibetunnel-config
  namespace: vibetunnel
data:
  PORT: "4021"
  NODE_ENV: "production"
  LOG_LEVEL: "INFO"
  # Add other non-sensitive configuration here
